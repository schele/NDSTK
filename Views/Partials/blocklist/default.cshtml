@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<Umbraco.Cms.Core.Models.Blocks.BlockListModel>
@{
    if (Model?.Any() != true) { return; }
}
<div class="umb-block-list">
    @foreach (var block in Model)
    {
        if (block?.ContentKey == null) { continue; }
        var data = block.Content;

        @* PascalCased to match the partial filenames - see the comment at the same call in
           Root.cshtml for why the casing is not optional. *@
        @await Html.PartialAsync("blocklist/Components/" + data.ContentType.Alias.ToFirstUpperInvariant(), block)
    }
</div>
